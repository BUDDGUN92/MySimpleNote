/// An enumeration representing the different filters that can be applied to a list of notes.
///
/// The available filters are:
/// - `all`: Show all notes.
/// - `active`: Show only active notes.
/// - `completed`: Show only completed notes.
enum NoteListFilter {
  all,
  active,
  completed,
}
